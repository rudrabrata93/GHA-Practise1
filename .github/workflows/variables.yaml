name: Variable Workflow
on: workflow_dispatch
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: set variable
        id: set_var
        run: echo "Hello World!" echo 'var1={}' >> $GITHUB_STATE
      - name: print variable
        run: echo "${{ steps.out_var.var1 }}"
